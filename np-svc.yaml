apiVersion: v1
kind: Service
metadata:
  name: np-svc
spec:
  type: NodePort
  selector:
    name: pod-np
    type: nodeport
    env: dev-np
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30007
